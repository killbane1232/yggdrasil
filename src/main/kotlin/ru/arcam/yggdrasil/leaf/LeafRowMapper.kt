package ru.arcam.yggdrasil.leaf

//import org.springframework.jdbc.core.RowMapper
//import java.sql.ResultSet
//import java.sql.SQLException

class LeafRowMapper {//}: RowMapper<Leaf> {
    /**
     * Возвращает информацию о пользователе
     * @param rs запись в таблице user_info
     * @param rowNum номер записи
     * @return информация о пользователе
     * @throws SQLException если в таблице нет колонки
     *
    @Throws(SQLException::class)
    override fun mapRow(rs: ResultSet, rowNum: Int): Leaf {
        return Leaf(
            name = rs.getString("name"),
            attachedBranch = rs.getString("attached_branch")
        )
    }*/
}